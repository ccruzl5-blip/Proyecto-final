# --- Configuraci�n del Servidor Web ---
server.port=8080

# --- Configuraci�n de la Base de Datos MariaDB ---
spring.datasource.url=jdbc:mariadb://localhost:3306/proyecto_atletas
spring.datasource.username=root
spring.datasource.password=Coquit02006
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# --- Configuraci�n de JPA ---
# Le dice a Spring que use el "dialecto" de MariaDB
spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect

# "update": Spring intentar� actualizar tus tablas si cambias el c�digo.
# Es muy �til durante el desarrollo.
spring.jpa.hibernate.ddl-auto=update

# Muestra los comandos SQL en la consola
spring.jpa.show-sql=true